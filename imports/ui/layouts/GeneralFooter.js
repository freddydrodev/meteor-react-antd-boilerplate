import React, { Component } from 'react'
import { Layout } from 'antd'

const { Footer } = Layout

export default class GeneralFooter extends Component {
    render() {
        return <Footer>General Footer</Footer>
    }
}
